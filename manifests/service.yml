apiVersion: v1
kind: Service
metadata:
    name: "atomaks-77fe"
    labels:
        app: "atomaks-77fe"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "atomaks-77fe"