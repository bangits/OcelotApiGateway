apiVersion: v1
kind: Service
metadata:
    name: "atomconstruct-ccb3"
    labels:
        app: "atomconstruct-ccb3"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "atomconstruct-ccb3"