apiVersion: v1
kind: Service
metadata:
    name: "ocelot-gateway"
    labels:
        app: "ocelot-gateway"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ocelot-gateway"